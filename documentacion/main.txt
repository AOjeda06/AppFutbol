Archivo: main.js
Descripción: Este archivo inicializa la aplicación cargando los datos iniciales desde archivos JSON y los procesa utilizando la clase `Model`.

Funciones:

1. Nombre: `cargarDatosInicialesDesdeArchivos`
   Descripción: Carga datos iniciales desde una lista de archivos JSON y los procesa utilizando el método `cargarDatosIniciales` de la clase `Model`.
   Parámetros de entrada: Ninguno.
   Retorno: Ninguno (es una función `async` que realiza operaciones asíncronas).

Proceso principal:
- Se ejecuta una función autoejecutable que realiza los siguientes pasos:
  1. Llama a `cargarDatosInicialesDesdeArchivos` para cargar y procesar los datos iniciales desde archivos JSON.
  2. Maneja errores en caso de que los archivos no se encuentren o haya problemas al procesarlos.
